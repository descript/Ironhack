<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="styles.css">
    <title>Movie Lovers Unite!</title>
  </head>
  <header><h1>Movie Lovers Unite!</h1></header>
  </head>
  <body>
    <div>
      <% randomhashpair = Hash[@questions.to_a.sample(1)] %>
    <p><%=randomhashpair.values[0][0]%></p>
  </div>
  <% index = 0 %>
  <% @list.each do |movie| %>
  <form action="/answer/<%=randomhashpair.keys[0]%>" method="post">
  <input type="image" name="answer" value="<%=movie.title%>" style="width: 200px; height: 300px" class="movie_tile" src="<%= movie.poster %>"/>
  </form>
  <%end%>
  </body>
</html>
